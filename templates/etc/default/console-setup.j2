# CONFIGURATION FILE FOR SETUPCON

# Consult the console-setup(5) manual page.

ACTIVE_CONSOLES="{{ etcdefaults_console_setup_active_consoles }}"

CHARMAP="{{ etcdefaults_console_setup_charmap }}"

CODESET="{{ etcdefaults_console_setup_codeset }}"
FONTFACE="{{ etcdefaults_console_setup_fontface }}"
FONTSIZE="{{ etcdefaults_console_setup_fontsize }}"

{% if etcdefaults_console_setup_videomode %}
VIDEOMODE="{{ etcdefaults_console_setup_videomode }}"
{% else %}
VIDEOMODE=
{% endif %}

# The following is an example how to use a braille font
# FONT='lat9w-08.psf.gz brl-8x8.psf'
{% if etcdefaults_console_setup_font %}
FONT="{{ etcdefaults_console_setup_font }}"
{% endif %}
