# CONFIGURATION FILE FOR SETUPCON

# Consult the console-setup(5) manual page.

ACTIVE_CONSOLES="{{ etcdefaults.console_setup.active_consoles }}"

CHARMAP="{{ etcdefaults.console_setup.charmap }}"

CODESET="{{ etcdefaults.console_setup.codeset }}"
FONTFACE="{{ etcdefaults.console_setup.fontface }}"
FONTSIZE="{{ etcdefaults.console_setup.fontsize }}"

{% if etcdefaults.console_setup.videomode %}
VIDEOMODE="{{ etcdefaults.console_setup.videomode }}"
{% else %}
VIDEOMODE=
{% endif %}

# The following is an example how to use a braille font
# FONT='lat9w-08.psf.gz brl-8x8.psf'
{% if etcdefaults.console_setup.font %}
FONT="{{ etcdefaults.console_setup.font }}"
{% endif %}
